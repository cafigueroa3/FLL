# Copia este archivo a ".env" (en esta misma carpeta) y pon tu propia contraseña.
# ".env" está en .gitignore, así que nunca se sube al repo.
# En Railway esta variable se define en su panel de Variables, no en un .env.
HEAD_PASSWORD=pon-aqui-tu-contraseña
